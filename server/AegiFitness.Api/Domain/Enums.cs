namespace AegiFitness.Api.Domain;

public enum Goal { Recomposition, Bulk, Cut }

public enum Modality { Rest, Gym, Calisthenics, Both }

public enum GymMode { Bodybuilding, Health, Combined }

public enum CalisthenicsMode { Classic, Military, CrossFit }

public enum MuscleGroup { Chest, Back, Legs, Shoulders, Biceps, Triceps, Core }

public enum MealType { Breakfast, Lunch, Dinner, Snack }

public enum CatalogObjective { Bulk, Cut, Both }

public enum Sex { Male, Female }

public enum LicenseStatus { Pending, Active, Suspended, Expired, Revoked }

public enum AchievementCategory { Training, Nutrition, Consistency, Progress, Special }

public enum GoalType { TargetWeight, WeeklyWorkouts, DailyProtein, Custom }

public enum GoalStatus { Active, Completed, Abandoned }
